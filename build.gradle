/*
 * This file was generated by the Gradle 'init' task.
 */

allprojects {
    group = 'com.scottlogic.deg'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'

    repositories {
        mavenLocal()
        maven {
            url = 'http://repo.maven.apache.org/maven2'
        }
    }

    sourceCompatibility = '1.8'

    publishing {
        publications {
            maven(MavenPublication) {
                from(components.java)
            }
        }
    }

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
	
	task('retest') {
		dependsOn {
			cleanTest
		}
		finalizedBy {
			test
		}
	}
}

task release(){
	if (project.version == "unspecified") {
		def message = "No version is specified on the command line, use `gradle -Pversion=0.1 release` to specify a version, will use 0.0 as the default"
		def out = services.get(org.gradle.internal.logging.text.StyledTextOutputFactory).create("")
		def warning = out.withStyle(org.gradle.internal.logging.text.StyledTextOutput.Style.Failure)
		
		warning.println(message)
	}
}
