### When creating a feature file certain syntax should be used:

### Generation strategy:

This can be either full, interesting or random:

i.e: 
Background:
    Given the generation strategy is full
   
### Setting field value and the data which should/shouldn't be generated

- Given there is a field foo
- Then the following data should be included in what is generated:
- Then the following data should not be included in what is generated:

### Correct date format 

- YYYY-MM-DDThh:mm:ss.nnn
