version: 2 
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    steps:
      - checkout
      - run: mvn package
workflows:
  version: 2
  build_and_test: 
    jobs:
      - build
