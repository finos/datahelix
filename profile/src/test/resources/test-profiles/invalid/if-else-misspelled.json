{
  "schemaVersion": "0.16",
  "rules": [
    {
      "rule": "rule 1",
      "constraints": [
        {
          "if": {"field": "foo", "is": "ofType", "value": "integer"},
          "then": {"field": "foo", "greaterThan": 0},
          "gelse": {"field": "foo", "equalTo": "N/A"}
        }
      ]
    }
  ],
  "fields": [
    {
      "name": "field1"
    }
  ]
}