package com.scottlogic.deg.profile.validators;

import com.scottlogic.deg.common.validators.ValidationResult;
import com.scottlogic.deg.common.validators.Validator;
import com.scottlogic.deg.profile.commands.CreateConstraint;

public class CreateConstraintValidator implements Validator<CreateConstraint>
{
    @Override
    public ValidationResult validate(CreateConstraint createConstraint)
    {
        return null;
    }
}
